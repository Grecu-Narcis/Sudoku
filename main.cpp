#include "Sudoku.h"

int main() {
	//clock_t start = clock();

	Sudoku Game;
	Game.menu();
	/*char sir[] = "sudoku.in";
	Game.readTable(sir);
	Game.printTable();*/


	//clock_t stop = clock();

	//cout << 1. * (stop - start) / CLOCKS_PER_SEC;

	return 0;
}